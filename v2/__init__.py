"""Finance 2.0 — FastAPI v2 namespace.

Lives alongside the existing single-file `main.py` (v1 endpoints
/health, /analyze, /uploads). Mounted in main.py with prefix `/api/v2`.
All new endpoints, schemas, services, parsers go here.
"""
